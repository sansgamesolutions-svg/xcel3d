
### Builder (`Builder/`)

Pure-virtual accessor interfaces for importing FEM/3D model data. Not yet connected to the Graphics layer.

- `ModelAccessor` — visits lights and parts
- `PartAccessor` — provides material, mesh, and skeleton per renderable part